# Hướng Dẫn Định Dạng Viết Tutorial


## 1. Các thẻ Markdown cơ bản
- `**In đậm**`: Dùng `**chữ in đậm**` hoặc `__chữ in đậm__`
- `*In nghiêng*`: Dùng `*chữ in nghiêng*` hoặc `_chữ in nghiêng_`
- `~~Gạch ngang~~`: Dùng `~~chữ gạch ngang~~`
- `[Đường link tới Tutorial khác](cach-vao-quickcpy-max-tren-fx580vnx.txt)`: Dùng `[Tên hiển thị](link)`
- `Trích dẫn:`
> Dùng dấu `>` ở đầu dòng để tạo một khung trích dẫn.

## 2. Khung Code
Để viết một đoạn code, hãy bọc chúng trong 3 dấu backtick (\`\`\`). Bạn cũng có thể chỉ định ngôn ngữ ở sau.
```txt
ADDR E9E0:
3A 25 3B 23
```

## 3. Các phím bấm Casio (Markdown đặc biệt)
Bạn có thể dùng các markdown như sau:

- Phím bấm máy tính cơ bản:

```
` ` `key-fx580vnx
C
` ` `
```
-> sẽ render ra một khối giao diện nút bấm.

Ví dụ: 
```key-fx580vnx
C
```

- Kí hiệu đặc biệt hoặc chữ số trên màn hình:
```
` ` `font-fx580vnx
▸Simp
` ` `
```

Ví dụ:
```font-fx580vnx
▸Simp
```

## 4. Dấu phân cách
Dùng 3 dấu gạch ngang `---` để tạo một đường kẻ ngang chia cắt nội dung.

---

Sau khi hoàn thành file của bạn hãy gửi cho admin luongvantam để up lên đây. Chúc bạn viết tài liệu hiệu quả!
